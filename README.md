# rkali090.github.io

Default GitHub Pages website built with Next.js static export.

## Local development

```bash
pnpm install
pnpm dev
```

## Build

```bash
pnpm build
```

The static site is exported to `out/` and deployed to GitHub Pages by `.github/workflows/pages.yml`.
